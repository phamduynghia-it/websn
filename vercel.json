{
    "buildCommand": "npm run build",
    "outputDirectory": ".",
    "installCommand": "npm install",
    "framework": null,
    "routes": [
        {
            "src": "/src/(.*)",
            "dest": "/src/$1"
        },
        {
            "src": "/dist/(.*)",
            "dest": "/dist/$1"
        },
        {
            "src": "/(.*)",
            "dest": "/index.html"
        }
    ]
}
